# BSDSF23M002-OS-A03

## Feature 1 – Base Shell (Release 1)

This is the starting point for the Operating Systems Programming Assignment 03.  
It implements a very basic command-line shell that can:
- Display a prompt
- Accept user commands
- Execute external commands using `fork()` and `execvp()`
- Exit when the user types `exit`

